<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.NutriPath" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Avoids a white flash before Compose draws; resolves to dark in values-night. -->
        <item name="android:windowBackground">@color/np_bg</item>
    </style>
</resources>